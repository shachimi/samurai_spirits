#ifndef FAMILY_HH_
# define FAMILY_HH_

class Family {
public:
    Family(void);
    ~Family(void);

    /* Getter and setters */
    // Bonus * getBonus(void) const { return this->bonus; };
    // void setBonus(Bonus * bonus) { this->bonus = bonus; };

private:
    // Bonus * bonus;
};

#endif /* !FAMILY_HH_ */
